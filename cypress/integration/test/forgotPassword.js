describe("Actions", () => {
    beforeEach(() => {
        cy.visit("/forgot_password") //Specify extra URL folder structure if needed between apostrophes 
    })

    it('Forgotten Password', () => {






        
    })

})