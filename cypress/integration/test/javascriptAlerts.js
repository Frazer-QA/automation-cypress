describe("Actions", () => {
    beforeEach(() => {
        cy.visit("/javascript_alerts") //Specify extra URL folder structure if needed between apostrophes 
    })

    it('JS Alert', () => {

        cy.get('')
        

    })

})